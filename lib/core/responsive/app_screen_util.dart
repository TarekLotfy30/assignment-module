import 'package:flutter/material.dart';

class AppScreenUtil {
  /// Design size for ScreenUtilInit
  static const Size designSize = Size(390, 845);

  /// Maximum text scale factor to prevent UI overflow
  static const double maxTextScaleFactor = 1.3;
}
